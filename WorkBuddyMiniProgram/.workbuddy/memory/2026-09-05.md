# 2026-09-05

## WeChat Mini Program 中文拼音助手 — Phase 2 bug fixes (resumed)

Resolved the 4 runtime issues reported via screenshot:

1. **Engine never ready ("拼音库仍在加载")** — fixed in `miniapp/app.js` (`_loadOne`/`_fireReady`/`onReady`) in prior session (Task #9). Verified.
2. **Status bar overlap** — `miniapp/pages/index/index.js`: read `wx.getSystemInfoSync().statusBarHeight` in `onLoad`, append `padding-top:<h>px` to the container style in `_initTheme` (page uses `navigationStyle: custom`). Only index needs it; settings/about use default nav.
3. **Privacy footer two centered lines** — added `white-space: pre-line` to `.privacy-tip` (and `.about-line`) in `app.wxss`; inserted a `\n` line break in every language's `footer.privacy`/`settings.about.privacy`.
4. **5 new languages** — added full `fr/es/pt/ko/hi` tables to `miniapp/utils/i18n.js`, added to `SUPPORTED`, added detection in `detectSystemLang`, and added options to `settings.js` language picker.

### Key gotcha (reusable)
The Edit/Write tool converts a literal `\n` in the parameter into a real newline in the file, which breaks JS string literals. To embed a backslash-n escape inside source via tool edits, the harness preserved `\\n` (typed as `\\n` in the new_string) as a proper `\n` escape — but this was inconsistent (a separate `replace_all` turned `\n` into a real newline). Safe pattern: fix multi-line strings with a Python script writing `'\\n'` (backslash-n) to the file, never a bare `\n`. Apostrophes `\'` were preserved correctly.

### Verification
- `node -c` passes on app.js, i18n.js, settings.js, index.js, pages/settings/settings.js.
- Runtime smoke test (wx stub): 9 supported langs, all have complete key sets, every `footer.privacy` contains `\n`, fallback returns key for unknown.

## Follow-up round (same day) — second screenshot showed issues still present

Root causes that the earlier fixes missed:

1. **Engine still never ready** — `require.async` for subpackages was *hanging* on the real device (no resolve, no reject), so the fallback to `wx.loadSubpackage` never triggered. Fixed `miniapp/app.js`: reversed priority — `wx.loadSubpackage` is now primary with an 8s timeout; `require.async` is only a fallback. Promises are settled exactly once (`settleOnce`) to avoid double resolve/reject from the timeout + success race.

2. **Header still overlaps status bar** — my `_statusBarHeight` capture existed but I forgot to actually *use* it in `_initTheme` (the `if (this._statusBarHeight)` guard silently skipped when undefined). Now it appends `padding-top:<h>px` to `themeStyle`. Also added right-side safe area: `wx.getMenuButtonBoundingClientRect()` → header `padding-right = screenWidth - capsule.left + 8`, so the ⓘ/⚙ buttons clear the WeChat capsule.

3. **Language picker shows raw keys** — the original 4 tables (`zh-CN/zh-TW/en/ja`) were missing `settings.language.fr/es/pt/ko/hi`. The i18n fallback chain (`current → zh-CN → key`) returned the key string when nothing had it. Fixed with a Python script that inserted all 5 missing keys into each of the 4 tables (values use the native name for each language, not translated).

4. **Privacy footer two lines** — confirmed working (has `\n`, renders two lines under `white-space: pre-line`).

### Gotcha for next time
When using `wx.getMenuButtonBoundingClientRect()` in `onLoad`, check that `rect.left` is truthy — the API can return a zero-shape object on dev tools where the capsule doesn't exist, and `screenWidth - 0 + 8` would give a huge right padding. Always gate on `rect && rect.left`.

## Third round (same day) — "拼音库加载失败，请重启小程序重试"

User reported the convert button stays gray with "拼音库加载失败，请重启小程序重试" — i.e. the FAILURE path now fires fast instead of hanging.

**Root cause:** `miniapp/app.js` `loadAll()` iterated `pkg.files` and called `_loadOne(pkg, f)` **per file**, so `wx.loadSubpackage({ name: 'data' })` fired twice concurrently (once for `pinyin.js`, once for `zdic.js`); `fontA` fired twice too. WeChat's second concurrent `loadSubpackage` for the same package name never resolves/rejects → 8s timeout → `Promise.all` rejects → `onLaunch` `.then(_, err)` sets `globalData.loadError` → gray button + failure toast.

**Fix:** Added per-subpackage dedup in `miniapp/app.js`:
- New `subpackagePromises` cache + `_ensureSubpackage(pkgName)` → `wx.loadSubpackage` is invoked **exactly once per package name**; subsequent files in the same package reuse the resolved Promise.
- Rewrote `_loadOne` to `await _ensureSubpackage(pkg.name)` then `require(reqPath)` (prefers sync require once package is ready; `require.async` only as a fallback if sync throws).
- Timeout raised 8s → 10s; header comment updated.

**Verification:**
- `node -c miniapp/app.js` → SYNTAX_OK.
- Sandbox test (real app.js, stubbed `wx`/`require`): loadSubpackage call counts = `{"data":1,"phrase":1,"fontA":1,"fontB":1}` (each package once); `globalData.ready=true`, `loadError=null`, engine set, `wordCount=2` → RESULT_OK.
- Re-ran i18n picker smoke test (9 langs × 9 options): no raw keys; `footer.privacy` has `\n` in all → I18N_OK.

**Key reusable lesson:** In WeChat Mini Programs, never call `wx.loadSubpackage({ name: X })` more than once for the same package — dedup concurrent requests behind a single cached Promise.

## Fourth round (same day) — "拼音库加载失败，请重启小程序重试" PERSISTED

The dedup fix from round 3 made `wx.loadSubpackage` behave, but the failure still persisted: the button stayed gray with the same error. Real failure mode on the device was the **synchronous `require(reqPath)` inside `_loadOne` throwing immediately** (subpackage success callback fires, then `require` throws synchronously), so the dedup Promise resolved but the per-file `require` rejected → `Promise.all` rejected → `loadError` set.

**Real root cause:** `miniapp/app.js _loadOne` used a **dynamic `require` path** built by string concatenation:
```js
var reqPath = './' + pkgRoot + '/' + rel;
loadedModules[key] = require(reqPath);   // 动态拼接
```
WeChat's bundler (especially with `lazyCodeLoading: "requiredComponents"` in `app.json`) cannot statically resolve a runtime-concatenated `require` of a subpackage module — the module record is not emitted into the subpackage bundle in a way that `require` can look up by dynamic string. Result: `require` throws "module not found", `loadAll()` rejects, button grays forever.

**Fix in `miniapp/app.js`:**
1. Added `_requireLiteral(key)` — a `switch` returning a **static/literal `require`** for each known subpackage key (6 files: pinyin/zdic/phrase/font-part1/2/3). Bundler now sees literal requires and correctly emits module records into the proper subpackages.
2. `_loadOne` now calls `_requireLiteral(key)`; `require.async` fallback kept for old base libraries.
3. Extracted `_startLoad(app)` from `onLaunch` so the flow is reusable.
4. Added `App.reload(cb)` — clears `subpackagePromises` and `loadedModules`, re-runs `_startLoad`, and fires `readyCallbacks` on settlement. Pages can retry loading without restarting the mini program.

**UX improvements so users don't get stuck again:**
- `miniapp/pages/index/index.js`: new `loadFailed` flag. `_onReady` now shows the real `loadError.message` in the status bar (was just a generic "请重启小程序重试"). Added `onRetry` that calls `app.reload(_onReady)`.
- `miniapp/pages/index/index.wxml`: when `loadFailed`, the "转换" button is replaced by "重试加载" (calls `onRetry`).

**Verification:**
- `node -c miniapp/app.js` and `node -c miniapp/pages/index/index.js` → SYNTAX_OK.
- Full simulation (stubbed `wx`/`App`/`getApp`): `globalData.ready=true`, `loadError=null`, `wordCount=52990`, `engine` initialized, `engine.convert('中国银行')` → `中(zhōng)国(guó)银(yín)行(háng)`. RESULT_OK.

**Reusable lesson (CRITICAL):** In WeChat Mini Programs, **subpackage modules must always be `require`-ed via a literal/string-constant path**, never via runtime concatenation (`'./' + a + '/' + b`). With `lazyCodeLoading` enabled, the bundler won't emit a discoverable module record for dynamic requires, and runtime `require` will throw. Use a `switch`/`map` to dispatch on a known key to a literal `require()` call.

## UI/UX 调整（截图反馈）

用户基于截图提出 6 点调整：

1. **一屏内展示，禁止整页上下滑动** — `miniapp/pages/index/index.wxml` 给根容器加 `.home`，并把输入、输出两个卡片放在同一屏；`index.wxss` 中 `.home { height: 100vh; overflow: hidden; }`，输出卡片 `.output-card { flex: 1 1 0%; min-height: 0; }`，输出滚动区 `.output-scroll { flex: 1 1 0%; min-height: 0; }`，仅结果区内部滚动。
2. **去掉 ü→v 开关** — 从首页移除了 `.uv-row`（含 switch），把该选项只保留在设置页；首页获得额外纵向空间。
3. **去掉底部隐私提示** — 移除了首页的 `.privacy-tip`。
4. **设置页合理布局、按钮文字不换行** — `app.wxss` `.segment-item` 加 `white-space: nowrap; overflow: hidden; text-overflow: ellipsis;`；`settings.wxss` 加 `.settings-value` 与 `.settings-btn` 的 `white-space: nowrap`。
5. **首页设置/关于按钮放大并重新安排** — `index.wxss` 把 `.header-btn` 从 56rpx 放大到 72rpx，图标从 30rpx 放大到 40rpx，按钮间距 16rpx；保持右置但视觉上更易点击。
6. **DOCX 在微信内置查看器乱码，WPS 正常** — 无法在微信端直接修复内置查看器字体/编码兼容性问题。在设置页新增「导出说明」区块：`settings.wxml` 加静态说明，`settings.js` 加 `exportNoteTitle/exportNoteDocx`，`utils/i18n.js` 用脚本向 9 种语言都补了对应文案，说明 Word 文件建议用 WPS Office 打开。

### 顺手修复

设置页语言选择器原来只写了 4 个 `wx:if`（zh-CN/zh-TW/en/ja），新增 5 种语言后选择器当前值会空白。修复：`settings.js` 给语言行加 `displayValue: t('settings.language.' + s.lang)`；`settings.wxml` 用 `{{rows[0].displayValue}}` 并把 picker 的 `value` 索引扩展到 9 种语言。

### 验证
- `node -c` passes on `app.js`, `pages/index/index.js`, `pages/settings/settings.js`, `utils/i18n.js`.
- 启动模拟：ready=true, wordCount=52990, loadError=null. ✅

**Reusable lesson:** 微信小程序首页要「一屏内展示」时，给根容器定高 `100vh` + `overflow:hidden`，把需要滚动的内容包进内部 `scroll-view`，并通过 `flex: 1 1 0%; min-height:0` 让滚动区占据剩余空间。

## 第二轮 UI 微调（截图反馈）

用户又提出 5 点：

1. **关于/设置按钮靠右对齐** — `app.wxss .header-actions` 加 `margin-left: auto;` 把操作组推到最右侧。
2. **设置图标和关于图标一样大** — `app.wxss` 把 `.header-btn` 放大到 68rpx，图标统一 40rpx 并 `font-weight: 600`；给设置图标加 `.icon-gear`，单独放大到 46rpx 并微调 `translateY`，补偿 ⚙ 在字体中视觉偏小的问题。
3. **设置项标签与按钮间距太大、文字显示不全** — 根因是 `.settings-label { flex: 1 }` 把标签撑开，`.settings-value` 又被内联 `style="flex:0 0 auto;"` 固定，导致剩余空间全变成间距。修复：`app.wxss` 改 `.settings-label { flex: 0 0 auto; max-width: 42%; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }`、`.settings-value { flex: 1 1 auto; min-width: 0; text-align: right; }`；`settings.wxml` 去掉所有 `style="flex:0 0 auto;"`。现在按钮会占据右侧剩余空间，文字更完整。
4. **切换语言后「恢复默认」和导出说明未刷新** — `settings.js` 的 `i18n.onChange` 原来只调 `_renderRows()` 和 `theme.refresh()`，没重建 `data.i18n`。修复：`settings.onChange` 与 `i18n.onChange` 统一调用 `self._init()`（会重建 `i18n`、rows、themeStyle、导航标题），并用 `_wired` 防止重复注册。
5. **修改字号后页面字体无变化** — 字号由 `utils/theme.js` 输出 `--base-font` 等 CSS 变量驱动，但 `index.js`/`settings.js` 的 `settings.onChange` 回调没有调用 `theme.refresh()`。修复：`index.js settings.onChange` 加 `self._initTheme()`；`settings.js settings.onChange` 改调用 `self._init()`（内部已含 `theme.refresh()`）。

### 验证
- `node -c` passes on `app.js`, `pages/index/index.js`, `pages/settings/settings.js`, `utils/i18n.js`, `utils/theme.js`.
- 启动模拟：ready=true, loadError=null. ✅

**Reusable lesson:** 微信小程序里，字号/主题等 CSS 变量变化后，必须在对应页面的 `settings.onChange` 回调里重新调用 `theme.refresh()` 并把新的 `themeStyle` setData，否则 CSS 变量不会更新。同时，语言切换要重建整个 `i18n` 数据块，而不能只刷新 rows。

## 第三轮顶部导航调整（截图反馈）

用户要求：
1. **关于入口放到设置菜单里，topbar 上的关于按钮去掉。**
2. **设置按钮图标改回上一个版本并居中显示，设置按钮靠右侧对齐。**
3. **topbar 蓝色背景覆盖整个顶部区域，参考设置页系统导航栏样式。**

**修复：**
- `pages/index/index.wxml`：移除关于按钮（ⓘ），只保留设置按钮；设置图标去掉 `.icon-gear` 特殊类，恢复为普通 `⚙`（40rpx、居中）。
- `pages/index/index.js _initTheme`：不再给 `.container` 加 `padding-top`，改由 `.header` 自己承担状态栏高度。
- `pages/index/index.wxml`：`.header` 内联样式改为 `padding-top: {{statusBarHeight}}px; padding-right: {{headerRightPad}}px;`。
- `pages/index/index.wxss`：`.home .header { margin: 0 -24rpx; border-radius: 0; height: auto; min-height: 96rpx; padding: 0 20rpx 16rpx; }`，让蓝色头图横向铺满屏幕、顶部延伸到状态栏下方。
- `pages/settings/settings.wxml`：新增「关于」静态区块，显示应用名 + 版本号 + 右箭头。
- `pages/settings/settings.js`：新增 `goAbout` 方法；`i18n` 数据加 `aboutSectionTitle`、`aboutLabel`、`aboutVersion`。
- `pages/settings/settings.wxss`：加 `.settings-chevron` 箭头样式。

### 验证
- `node -c` passes on `pages/index/index.js`、`pages/settings/settings.js`.
- 启动模拟：ready=true。✅

**Reusable lesson:** 微信小程序自定义导航栏要「蓝色背景覆盖整个顶部」时，不要给根容器加 `padding-top`，而是让 header 自己设置 `padding-top: statusBarHeight`、并用负边距 `margin: 0 -containerPadding` 横向铺满，同时 `border-radius: 0` 以匹配系统导航栏的矩形效果。

## 逐字标注排版微调（截图反馈）

用户要求：
1. 拼音再紧凑一点。
2. 数字、英文、标点等不需要拼音的文字必须和中文在同一行。

**修复（`app.wxss` 逐字标注样式）：**
- `.ann-line { margin-bottom: 16rpx → 8rpx }`：行间距减半。
- `.ann-unit { margin-right: 4rpx → 2rpx }`：字单元间距减半。
- `.ann-pinyin { line-height: 1.3 → 1.1; letter-spacing: 0.5rpx → 0 }`：拼音行更紧凑。
- `.ann-char { line-height: 1.4 → 1.2 }`：汉字行更紧凑。
- `.ann-plain { line-height: 1.6 → 1.2; align-self: center → flex-end }`：非汉字文本与汉字底边对齐，确保在同一视觉行上，不会因为高度差异被挤到另一行。

这些调整没有改变引擎分词逻辑（非 CJK 和 CJK 仍在同一 `PinyinLine` 的 `tokens` 中），只是通过更紧凑的字号和更紧的行距让混合文本在一行内更自然地并排显示。

### 验证
- `node -c` passes on `pages/index/index.js`、`utils/pinyin.js`、`app.wxss` 语法正常。
- 启动模拟：ready=true。✅

## 第四轮 UI 调整（截图反馈 - 6 项）

用户提出 6 项要求：

1. **`option-label` 宽度加大** — `app.wxss .option-label`: `width:84rpx` → `width:auto; min-width:84rpx; max-width:40%;` 加 nowrap/ellipsis。
2. **`headerSubtitle` 去掉「（带声调）」** — 用 Python 脚本批量从 `utils/i18n.js` 的 9 个语言表中删除 `header.subtitle` 的括号修饰（zh-CN/zh-TW/en/ja/fr/es/pt/ko/hi）。
3. **底部 status-bar 文案未跟随语言切换** — `pages/index/index.js` 新增 `_refreshStatus()`，根据 `loadFailed/ready/_result` 三态生成当前语言下的 status 文案；在 `i18n.onChange`/`settings.onChange`/`onLoad`/`onReady` 全部回调里调用，保证文案始终与当前语言一致。
4. **设置/关于页标题放大到 1.5 倍** — 见下「**自定义导航栏改造**」一段。
5. **`.settings-label` 与 `.settings-value` 间距过大** — 见下「**一屏化设置页**」一段。
6. **设置页所有选项尽量在一页内显示** — 见下「**一屏化设置页**」一段。

### 自定义导航栏改造（任务 4）

- `pages/settings/settings.json` 和 `pages/about/about.json` 添加 `"navigationStyle": "custom"`。
- 两个页面 WXML 顶部新增 `.page-navbar`：左侧返回按钮（`‹`）+ 中间标题 + 右侧等宽占位（让标题视觉居中）。
- 两个页面 JS：
  - onLoad 读取 `wx.getSystemInfoSync().statusBarHeight`。
  - data 增加 `navTitle`、`statusBarHeight`。
  - `_init`/`_render` 把 `navTitle` 设为当前语言的标题（设置：`t('settings.title')`；关于：`t('settings.section.about')`）。
  - 新增 `goBack` 方法：`wx.navigateBack({ delta:1, fail: wx.switchTab(...) })`，失败兜底回主页。
  - 移除原来的 `wx.setNavigationBarTitle` 调用（自定义导航栏下无效）。
- `app.wxss` 新增样式：
  - `.page-navbar { background: var(--primary); padding: 0 24rpx 18rpx; min-height: 88rpx; ... }`
  - `.page-navbar-title { color:#fff; font-size: 48rpx; font-weight:600; ... }` —— **1.5 倍标准标题（32rpx × 1.5）**。
  - `.page-navbar-back { 60rpx 圆形按钮，半透明白底 }`、`.page-navbar-back-icon { font-size: 44rpx }`、`.page-navbar-spacer { 60rpx 透明占位 }`。
- 关于页 WXML 因原本根元素是 `.settings`，加了一层 `<view style="{{themeStyle}}">` 包裹（让 navbar 满宽）。
- `pages/settings/settings.wxss` 加 `.settings > .page-navbar { margin: 0 -24rpx 16rpx; border-radius: 0; }` 让 navbar 在 settings 内也满宽。

### 一屏化设置页（任务 5、6）

`app.wxss` 中关于「设置/关于页」的所有 token 全面压缩：
- `.settings { padding: 24rpx 24rpx 50rpx → 16rpx 24rpx 32rpx }`
- `.settings-section { margin-bottom: 24rpx → 14rpx; border-radius: 22rpx → 20rpx }`
- `.settings-section-title { padding: 20rpx 28rpx 0 → 16rpx 24rpx 0; font-size: 24rpx → 22rpx }`
- `.settings-row { padding: 24rpx 28rpx → 18rpx 24rpx }`
- `.settings-label { max-width: 42% → 48%; margin-right: 16rpx → 8rpx }` —— **任务 5**：给 label 留更多空间，缩窄 label 与 value 之间的间距。
- `.settings-value { ...; max-width: 360rpx; text-overflow: ellipsis }` —— 把单行省略号提到全局。
- `.settings-actions { padding: 24rpx 28rpx 36rpx → 16rpx 0 8rpx }`
- `.settings-btn { padding: 22rpx 0 → 18rpx 0; border-radius: 18rpx → 16rpx }`
- `.about-card { padding: 32rpx 28rpx → 24rpx 24rpx; margin-bottom: 24rpx → 14rpx }`
- `.about-name { font-size: 36rpx → 34rpx }`
- `.about-engine { margin-top: 18rpx → 14rpx }`

`pages/settings/settings.wxss` 简化为只保留 `.settings > .page-navbar`（满宽负 margin）、`.segment-item`（紧凑 8rpx×14rpx）、`.settings-note-section`、`.settings-chevron`。
`pages/about/about.wxss` 简化为只保留 `page { background }`。

### 验证
- `node -c` passes on `pages/index/index.js`、`pages/settings/settings.js`、`pages/about/about.js`、`app.wxss`。
- 启动模拟：ready=true。✅

**Reusable lesson:** 微信小程序「标题 1.5 倍」要落实时，单纯改 CSS 不够——系统的 `navigationBarTitleText` 字号由微信控制，外部无法修改。必须切到 `navigationStyle: custom`，自己用 `<view class="page-navbar">` 实现导航栏并自由控制字号。配套要做三件事：① onLoad 读 statusBarHeight 给 navbar 让出顶部；② 留等宽右侧占位保证标题视觉居中；③ `wx.setNavigationBarTitle` 在 custom 模式下无效，记得去掉。

## 第五轮 UI 调整（截图反馈 - 5 项）

用户基于 3 张截图提出 5 项修改：

1. **设置页 topbar 蓝色背景未铺满** — 根因是之前把 `.page-navbar` 放在 `.settings` 内部，靠 `margin: 0 -24rpx` 撑满；在某些渲染下负 margin 仍受父级 padding 约束，导致两侧留缝。修复：`settings.wxml` 改成和 about 页一致的结构：根 `<view style="{{themeStyle}}">` → `.page-navbar` → `.settings` 包裹内容。移除 `settings.wxss` 里的 `.settings > .page-navbar` 负 margin 规则。
2. **设置/关于页标题字号过大** — 用户要求与首页标题大小一致。首页 `.header-title` 是 30rpx，因此 `app.wxss .page-navbar-title` 从 48rpx 改为 30rpx。
3. **底部 status-bar 默认不要显示「请输入中文后…」** — 修改 `utils/i18n.js` 的 `output.statusEngineReady`，9 种语言全部去掉后半句提示，仅保留「拼音库就绪 · 词条 {n}」类的就绪信息。
4. **设置页语言 picker 的取消/确认按钮未多语言化** — 微信小程序原生 `<picker>` 的取消/确认文案由系统语言决定，无法通过属性自定义。解决方案：弃用原生 picker，改成自定义底部弹窗：
   - `settings.wxml`：语言行改为 tappable，点击打开 `.picker-mask + .picker-modal`；弹窗内用 `scroll-view` 列出 9 个语言选项，底部「取消」「确认」按钮绑定 `common.cancel` / `common.confirm`（9 种语言已有）。
   - `settings.js`：新增 `langPickerVisible`、`langPickerTemp`，`onLangPickerOpen / onLangPickerSelect / onLangPickerCancel / onLangPickerConfirm` 控制弹窗；确认后才 `settings.set('lang', ...)`。
   - `utils/i18n.js`：为 9 种语言新增 `settings.langPickerTitle` 作为弹窗标题。
5. **关于页底部英文 footer 删除** — 直接移除 `pages/about/about.wxml` 中最后一个 `<view class="about-card" style="text-align:center;">`（Made with ❤ ...）。

### 验证
- `node -c` passes on `pages/settings/settings.js`、`pages/about/about.js`、`utils/i18n.js`。
- `python3 -c json.load` passes on `pages/settings/settings.json`、`pages/about/about.json`。

**Reusable lesson:** 微信小程序的 `<picker>` 组件不支持自定义取消/确认文案；若有多语言需求，必须自己用 modal + scroll-view/picker-view 实现选择器，并把按钮文案绑定到当前语言的 i18n 键。同时，自定义导航栏要实现真正满宽，最安全的方式是把它放到页面根 view 下，而不是放在带 padding 的 `.settings/.container` 内部再用负 margin 抵消。

## 第六轮 UI 微调（截图反馈 - 设置页分段按钮）

用户反馈「默认声调」「默认显示」两行分段按钮间距太大、文字被截断。

**修复：**
- `app.wxss .settings-value`：移除 `max-width: 360rpx` 限制，让 value 区域随 flex 容器自动伸展，给分段按钮更多可用宽度；保留 `text-overflow: ellipsis` 保险。
- `settings.wxss .segment-item`：`padding: 8rpx 14rpx → 6rpx 10rpx`，缩小单个按钮内边距。
- `settings.wxml`：所有 4 处分段容器 `style="display:flex; gap:8rpx"` 统一改为 `gap:4rpx`。

### 验证
- `node -c miniapp/pages/settings/settings.js` → OK。

**Reusable lesson:** 设置页里「左侧 label + 右侧分段按钮」这种布局，如果 `.settings-value` 加了 `max-width`，会人为把按钮容器压窄并拉大 label 与按钮之间的空白。对需要宽度的分段按钮行，应让 `.settings-value` 自然伸展或显著提高 max-width，并同步压缩按钮内边距与按钮间距。

## 第七轮 UI 调整（截图反馈 - 4 项）

用户基于截图提出 4 项修改：

1. **默认声调/默认显示分段按钮太拥挤** — 之前一轮为了防截断把 gap 压到 4rpx、padding 压到 6rpx×10rpx，现在文字已能完整显示，用户希望更舒展。修复：`settings.wxml` 4 处 segment 容器 `gap:4rpx → 8rpx`；`settings.wxss .segment-item` `padding:6rpx 10rpx → 8rpx 16rpx`。
2. **「恢复默认」按钮需要按钮背景** — 原来的 `.settings-btn.ghost` 是透明背景 + 灰色文字，看着不像按钮。修复：`app.wxss .settings-btn.ghost` 改为 `background: var(--primary); color:#fff; box-shadow: ...`，与主按钮视觉一致。
3. **语言选择弹窗按钮语言与当前语言不一致** — 用户在选择语言时，弹窗内的「取消/确认」和标题应即时反映当前高亮的语言（预览效果），而不是等确认后才刷新。修复：
   - `utils/i18n.js` 新增导出 `tLang(lang, key, vars)`：按指定语言取文案，不改动全局当前语言。
   - `pages/settings/settings.js` 数据新增 `pickerTitle / pickerCancel / pickerConfirm`；`onLangPickerOpen` 用当前语言填充，`onLangPickerSelect` 用临时选中的语言实时刷新这三个字段。
   - `pages/settings/settings.wxml` 弹窗标题和按钮改为绑定 `{{pickerTitle}}`、`{{pickerCancel}}`、`{{pickerConfirm}}`。

### 验证
- `node -c` passes on `pages/settings/settings.js`、`utils/i18n.js`、`pages/about/about.js`。

**Reusable lesson:** 当弹窗需要在「确认前」预览目标语言时，不要把全局语言切换作为唯一手段；应提供一个 `tLang(lang, key)` 接口，让弹窗按临时选中的语言渲染文案，确认后再调用 `setLang`/`settings.set('lang', ...)` 真正生效。

## 第八轮 UI 调整（截图反馈 - 2 项）

用户基于 2 张截图提出 2 项修改：

1. **设置页「主题」改成「主题颜色」** — 仅改文案，不改功能。`utils/i18n.js` 把 9 种语言的 `settings.theme` 全部替换：`zh-CN '主题颜色'`、`zh-TW '主題顏色'`、`en 'Theme Color'`、`ja 'テーマカラー'`、`fr 'Couleur du thème'`、`es 'Color del tema'`、`pt 'Cor do tema'`、`ko '테마 색상'`、`hi 'थीम रंग'`。settings.js/wxml 已经通过 `t('settings.theme')` 绑定标签，无需其它改动。
2. **关于页「反馈与信息」和「复制应用信息」没有使用多语言** — 这是页面里残留的两处硬编码中文。修复：
   - `utils/i18n.js`：在 9 种语言块中新增 `settings.about.sectionTitle` 和 `settings.about.copyInfo`（如 zh-CN `'反馈与信息'`/`'复制应用信息'`、en `'Feedback & Info'`/`'Copy app info'`、ja `'フィードバックと情報'`/`'アプリ情報をコピー'` 等）。
   - `pages/about/about.wxml`：把 `<view class="settings-section-title">反馈与信息</view>` 改成 `{{i18n.sectionTitle}}`；把 `<view class="settings-value">复制应用信息</view>` 改成 `{{i18n.copyInfo}}`。
   - `pages/about/about.js` `_render()`：在 `data.i18n` 中新增 `sectionTitle: t('settings.about.sectionTitle')` 和 `copyInfo: t('settings.about.copyInfo')`。

### 验证
- `node -c miniapp/pages/about/about.js && node -c miniapp/utils/i18n.js` → SYNTAX OK。
- `Grep` 确认 about 页不再含「反馈与信息」「复制应用信息」硬编码字符串，9 种语言块均含新增键。

**Reusable lesson:** 多语言 i18n 字符串库加完新键后，务必先 grep 整个 pages/ 目录确认页面是否已绑定 `{{i18n.xxx}}`，否则会出现「键存在但页面显示还是硬编码中文」的情况。可以把每个新增 i18n 键当成一个独立的 PR：先在 i18n.js 里补齐所有语言，再逐个页面扫一遍旧硬编码字符串。
